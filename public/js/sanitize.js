/**
 * Generated from src/util/sanitize.js. Do not edit by hand.
 * Regenerate with: npm run generate:sanitize
 */
(function () {
  'use strict';
  /**
   * HTML and URL sanitization (XSS mitigation). Single source for server and client.
   * - Server: import { escapeHtml } from './src/util/sanitize.js'
   * - Client: load /js/sanitize.js (generated by npm run generate:sanitize)
   */

  function escapeHtml(str) {
    if (str == null) return '';
    return String(str)
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;')
      .replace(/'/g, '&#39;');
  }

  /** Validates safe base64 for data: URLs (prevents attribute injection in <embed src="data:...">). */
  function isSafeBase64(str) {
    return typeof str === 'string' && /^[A-Za-z0-9+/=\s]+$/.test(str);
  }

  /** Validates https: only (prevents javascript: and data: in href/src). */
  function isSafeUrl(url) {
    try {
      return new URL(url).protocol === 'https:';
    } catch {
      return false;
    }
  }

  window.escapeHtml = escapeHtml;
  window.isSafeBase64 = isSafeBase64;
  window.isSafeUrl = isSafeUrl;
})();
